import { ReactNode } from "react"

type TabItemInfo = {
    href: string,
    title: string,
}

type SidebarItemInfo = {
    href: string,
    title: string,
}

type ProgressItemInfo = {
    href: string,
    title: string,
    icon: ReactNode,
}