{
  "name": "sellify-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "services/*",
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md,css,mjs,json}\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "git clean -xdf node_modules .pnpm-store .turbo/ .ruff_cache"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "3.6.0",
    "eslint": "^9.39.1",
    "turbo": "2.5.4"
  },
  "packageManager": "pnpm@10.12.4"
}
